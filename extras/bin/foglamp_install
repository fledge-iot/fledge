#!/bin/bash
#set -x


echo "Installing FogLAMP..."

# Check the foglamp user

# Copy executables in the bin directory
cp ${HOME}/FogLAMP/extras/bin/* ${HOME}/bin/.

# Build the environment
cd ${HOME}/FogLAMP/src/python
source build.sh -i
source build.sh -a

echo "Installation Completed."
exit 0
