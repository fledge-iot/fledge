####################
# DATABASE #
####################

database:
    config_storage: instance # env_variables | instance
    dev:
        host: localhost
        port: 5432
        user: postgres
        password: postgres
        db: foglamp
    prod:
        host: localhost
        port: 5432
        user: prod_user
        password: postgres5432
        db: foglamp
    test:
        host: localhost
        port: 5432
        user: postgres
        password: postgres
        db: foglamp_test

####################
# JWT #
####################

jwt:
   secret: your secret key # spec?
   algorithm: HMAC SHA256  # HMAC SHA256 | RSA
   expiration_delta_seconds: 60*60  # expiration date delta; add to current time, after which the token becomes invalid.
   # Description of exp and other reserved keys provided in corresponding RFC section https://tools.ietf.org/html/rfc7519#section-4.1
   rsa:
       private_key: null # signer
       public_key: null  # verifier

####################
# COAP #
####################

coap:
    port: 5683
    nah:






